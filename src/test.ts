// interface IProps {
//   first: string;
//   last?: string;
// }

import { Dog } from "./types/dog";

// (): void =>

// const createFullName = ({ first, last }: IProps): void => {
//   setUserName(first + ' ' + last)
// };`

// createFullName({`
//   first: "david",
//   // last: "rajcher",
// });

const dog: Dog = {};

const something = {};

export default something;
